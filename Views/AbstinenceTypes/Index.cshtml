@model IEnumerable<Labb1Theres.Models.AbstinenceType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AbstinenceName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AbstinenceName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AbstinenceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AbstinenceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AbstinenceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
